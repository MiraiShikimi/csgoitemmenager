import { AddItemsFilterPipe } from './add-items-filter.pipe';

describe('AddItemsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AddItemsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
