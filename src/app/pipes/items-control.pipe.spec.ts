import { ItemsControlPipe } from './items-control.pipe';

describe('ItemsControlPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemsControlPipe();
    expect(pipe).toBeTruthy();
  });
});
